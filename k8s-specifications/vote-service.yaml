apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: LoadBalancer
  ports:
  - name: "vote-service"
    port: 8080        # Service port exposed externally
    targetPort: 80    # Container port in your pod
  selector:
    app: vote
